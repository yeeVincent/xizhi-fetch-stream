{
  "private": true,
  "scripts": {
    "copy-files": "cp -r ./apps/Stream/dist/* ./dist",
    "build": "turbo run build && mkdir dist && pnpm run copy-files",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.0.14"
  },
  "packageManager": "pnpm@9.2.0",
  "name": "cy-fetch-stream-monorepo"
}
